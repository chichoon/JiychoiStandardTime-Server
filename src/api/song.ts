import { Request, Response } from 'express';

export function postNewSongs(req: Request, res: Response) {
  const {} = req.body;
  res.send();
}

export function putExistingSong(req: Request, res: Response) {
  const {} = req.body;
  res.send();
}

export function deleteExistingSong(req: Request, res: Response) {
  const {} = req.body;
  res.send();
}
